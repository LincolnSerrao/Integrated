# Optional ML stack for scanner inference on Windows.
# Recommended Python: 3.11 (64-bit) for best compatibility.

# Core numerical stack
numpy==1.26.4
scipy==1.14.1
scikit-learn==1.5.2
joblib==1.4.2
tqdm==4.66.5
pefile==2024.8.26
pandas==2.2.3

# Binary parsing backend used by EMBER features
lief==0.14.1

# PyTorch runtime (CPU wheel by default on most setups)
torch==2.5.1

# EMBER feature extractor
git+https://github.com/elastic/ember.git@master#egg=ember
